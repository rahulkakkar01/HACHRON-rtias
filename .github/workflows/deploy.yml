name: 🚀 Deploy Website to GitHub Pages

on:
  push:
    branches:
      - main  # Deploy when changes are pushed to 'main'
  workflow_dispatch:  # Allows manual deployment trigger

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔨 Setup Node.js (Required for Deployment)
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📦 Upload Website Artifacts (Fixing Deprecation Issue)
        uses: actions/upload-artifact@v4  # Updated version
        with:
          name: website-artifact
          path: ./web_application  # Save website files as artifacts

  deploy:
    needs: build  # Deploy job runs after build job
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Download Website Artifacts (Using New Version)
        uses: actions/download-artifact@v4  # Updated version
        with:
          name: website-artifact
          path: ./web_application  # Restore files from artifacts

      - name: 🚀 Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          publish_dir: ./web_application  # Deploy the 'web_application' folder

      - name: 🔗 Provide Deployment Link
        run: echo "Website deployed at https://PriyanshuKSharma.github.io/fantastic4/ "
